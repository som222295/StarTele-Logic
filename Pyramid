function pyramid() {
    var row = 4;
    var output = ' ';
    for (var i = 0; i <= row; i++) {
        for (var j = 0; j <= i; j++) {
            output += '*';
        }
        console.log(output);
        output = ' ';
    }
}

pyramid();
   
